-- Universal Hub by DevNameGelo | Powered by RGC
-- Full Loading Screen + Main UI with Toggle Circle Button

-- Services
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- Destroy old UI if exists
if PlayerGui:FindFirstChild("UniversalHub") then
    PlayerGui:FindFirstChild("UniversalHub"):Destroy()
end

-- Main ScreenGui
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "UniversalHub"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.IgnoreGuiInset = true
ScreenGui.Parent = PlayerGui

-- ============================================================
-- LOADING SCREEN (Full Screen - 3 seconds)
-- ============================================================

local LoadingFrame = Instance.new("Frame")
LoadingFrame.Name = "LoadingScreen"
LoadingFrame.Size = UDim2.new(1, 0, 1, 0)
LoadingFrame.Position = UDim2.new(0, 0, 0, 0)
LoadingFrame.BackgroundColor3 = Color3.fromRGB(5, 5, 15)
LoadingFrame.BorderSizePixel = 0
LoadingFrame.ZIndex = 100
LoadingFrame.Parent = ScreenGui

-- Background gradient for loading
local LoadingGradient = Instance.new("UIGradient")
LoadingGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(15, 0, 30)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(5, 5, 15)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 10, 30))
})
LoadingGradient.Rotation = 45
LoadingGradient.Parent = LoadingFrame

-- Animated particles background frame
local ParticleContainer = Instance.new("Frame")
ParticleContainer.Name = "Particles"
ParticleContainer.Size = UDim2.new(1, 0, 1, 0)
ParticleContainer.BackgroundTransparency = 1
ParticleContainer.ZIndex = 101
ParticleContainer.ClipsDescendants = true
ParticleContainer.Parent = LoadingFrame

-- Create floating particles
for i = 1, 30 do
    local particle = Instance.new("Frame")
    particle.Name = "Particle_" .. i
    particle.Size = UDim2.new(0, math.random(2, 6), 0, math.random(2, 6))
    particle.Position = UDim2.new(math.random() * 1, 0, math.random() * 1, 0)
    particle.BackgroundColor3 = i % 2 == 0 and Color3.fromRGB(255, 50, 80) or Color3.fromRGB(50, 120, 255)
    particle.BackgroundTransparency = math.random(3, 7) / 10
    particle.BorderSizePixel = 0
    particle.ZIndex = 101
    particle.Parent = ParticleContainer
    
    local particleCorner = Instance.new("UICorner")
    particleCorner.CornerRadius = UDim.new(1, 0)
    particleCorner.Parent = particle
    
    -- Animate particles floating up
    spawn(function()
        while particle.Parent do
            local targetPos = UDim2.new(math.random() * 1, 0, -0.1, 0)
            local tween = TweenService:Create(particle, TweenInfo.new(math.random(3, 8), Enum.EasingStyle.Linear), {
                Position = targetPos,
                BackgroundTransparency = 1
            })
            tween:Play()
            tween.Completed:Wait()
            particle.Position = UDim2.new(math.random() * 1, 0, 1.1, 0)
            particle.BackgroundTransparency = math.random(3, 7) / 10
        end
    end)
end

-- Center glow effect (red/blue vortex simulation)
local CenterGlow = Instance.new("Frame")
CenterGlow.Name = "CenterGlow"
CenterGlow.Size = UDim2.new(0, 200, 0, 200)
CenterGlow.Position = UDim2.new(0.5, -100, 0.45, -100)
CenterGlow.BackgroundColor3 = Color3.fromRGB(150, 30, 60)
CenterGlow.BackgroundTransparency = 0.5
CenterGlow.BorderSizePixel = 0
CenterGlow.ZIndex = 102
CenterGlow.Parent = LoadingFrame

local CenterGlowCorner = Instance.new("UICorner")
CenterGlowCorner.CornerRadius = UDim.new(1, 0)
CenterGlowCorner.Parent = CenterGlow

local CenterGlowGradient = Instance.new("UIGradient")
CenterGlowGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 50, 80)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(120, 30, 180)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(50, 120, 255))
})
CenterGlowGradient.Parent = CenterGlow

-- Outer glow ring
local OuterGlow = Instance.new("Frame")
OuterGlow.Name = "OuterGlow"
OuterGlow.Size = UDim2.new(0, 260, 0, 260)
OuterGlow.Position = UDim2.new(0.5, -130, 0.45, -130)
OuterGlow.BackgroundColor3 = Color3.fromRGB(80, 20, 150)
OuterGlow.BackgroundTransparency = 0.7
OuterGlow.BorderSizePixel = 0
OuterGlow.ZIndex = 101
OuterGlow.Parent = LoadingFrame

local OuterGlowCorner = Instance.new("UICorner")
OuterGlowCorner.CornerRadius = UDim.new(1, 0)
OuterGlowCorner.Parent = OuterGlow

-- Spinning ring animation
spawn(function()
    local rotation = 0
    while CenterGlow.Parent do
        rotation = rotation + 2
        CenterGlowGradient.Rotation = rotation
        
        -- Pulse effect
        local scale = 1 + math.sin(tick() * 3) * 0.05
        CenterGlow.Size = UDim2.new(0, 200 * scale, 0, 200 * scale)
        CenterGlow.Position = UDim2.new(0.5, -100 * scale, 0.45, -100 * scale)
        
        OuterGlow.Size = UDim2.new(0, 260 * scale, 0, 260 * scale)
        OuterGlow.Position = UDim2.new(0.5, -130 * scale, 0.45, -130 * scale)
        
        RunService.Heartbeat:Wait()
    end
end)

-- Crossed swords icon (center) using text
local SwordsIcon = Instance.new("TextLabel")
SwordsIcon.Name = "SwordsIcon"
SwordsIcon.Size = UDim2.new(0, 80, 0, 80)
SwordsIcon.Position = UDim2.new(0.5, -40, 0.45, -40)
SwordsIcon.BackgroundTransparency = 1
SwordsIcon.Text = "⚔"
SwordsIcon.TextColor3 = Color3.fromRGB(200, 180, 255)
SwordsIcon.TextSize = 50
SwordsIcon.Font = Enum.Font.GothamBold
SwordsIcon.ZIndex = 103
SwordsIcon.Parent = LoadingFrame

-- Title "Universal"
local TitleLabel = Instance.new("TextLabel")
TitleLabel.Name = "Title"
TitleLabel.Size = UDim2.new(0, 500, 0, 70)
TitleLabel.Position = UDim2.new(0.5, -250, 0.12, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "Universal"
TitleLabel.TextColor3 = Color3.fromRGB(200, 100, 150)
TitleLabel.TextSize = 60
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.ZIndex = 103
TitleLabel.Parent = LoadingFrame

local TitleGradient = Instance.new("UIGradient")
TitleGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(180, 50, 80)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(150, 80, 200)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(80, 150, 255))
})
TitleGradient.Parent = TitleLabel

-- Animate title gradient
spawn(function()
    local rot = 0
    while TitleLabel.Parent do
        rot = (rot + 1) % 360
        TitleGradient.Rotation = rot
        RunService.Heartbeat:Wait()
    end
end)

-- "By: DevNameGelo" subtitle
local SubtitleLabel = Instance.new("TextLabel")
SubtitleLabel.Name = "Subtitle"
SubtitleLabel.Size = UDim2.new(0, 400, 0, 30)
SubtitleLabel.Position = UDim2.new(0.5, -200, 0.19, 0)
SubtitleLabel.BackgroundTransparency = 1
SubtitleLabel.Text = "By: DevNameGelo"
SubtitleLabel.TextColor3 = Color3.fromRGB(180, 160, 220)
SubtitleLabel.TextSize = 24
SubtitleLabel.Font = Enum.Font.Gotham
SubtitleLabel.ZIndex = 103
SubtitleLabel.Parent = LoadingFrame

-- Loading text
local LoadingText = Instance.new("TextLabel")
LoadingText.Name = "LoadingText"
LoadingText.Size = UDim2.new(0, 600, 0, 50)
LoadingText.Position = UDim2.new(0.5, -300, 0.7, 0)
LoadingText.BackgroundTransparency = 1
LoadingText.Text = "LOADING... [Establishing RGC Connection]"
LoadingText.TextColor3 = Color3.fromRGB(255, 80, 100)
LoadingText.TextSize = 22
LoadingText.Font = Enum.Font.GothamBold
LoadingText.ZIndex = 103
LoadingText.Parent = LoadingFrame

-- Status text
local StatusText = Instance.new("TextLabel")
StatusText.Name = "StatusText"
StatusText.Size = UDim2.new(0, 700, 0, 30)
StatusText.Position = UDim2.new(0.5, -350, 0.75, 0)
StatusText.BackgroundTransparency = 1
StatusText.Text = "Initializing modules..."
StatusText.TextColor3 = Color3.fromRGB(150, 150, 180)
StatusText.TextSize = 16
StatusText.Font = Enum.Font.Gotham
StatusText.ZIndex = 103
StatusText.Parent = LoadingFrame

-- Progress bar background
local ProgressBarBG = Instance.new("Frame")
ProgressBarBG.Name = "ProgressBarBG"
ProgressBarBG.Size = UDim2.new(0.6, 0, 0, 8)
ProgressBarBG.Position = UDim2.new(0.2, 0, 0.82, 0)
ProgressBarBG.BackgroundColor3 = Color3.fromRGB(30, 30, 50)
ProgressBarBG.BorderSizePixel = 0
ProgressBarBG.ZIndex = 103
ProgressBarBG.Parent = LoadingFrame

local ProgressBarBGCorner = Instance.new("UICorner")
ProgressBarBGCorner.CornerRadius = UDim.new(1, 0)
ProgressBarBGCorner.Parent = ProgressBarBG

-- Progress bar outline glow
local ProgressBarOutline = Instance.new("UIStroke")
ProgressBarOutline.Color = Color3.fromRGB(80, 80, 150)
ProgressBarOutline.Thickness = 1
ProgressBarOutline.Transparency = 0.5
ProgressBarOutline.Parent = ProgressBarBG

-- Progress bar fill
local ProgressBarFill = Instance.new("Frame")
ProgressBarFill.Name = "ProgressBarFill"
ProgressBarFill.Size = UDim2.new(0, 0, 1, 0)
ProgressBarFill.Position = UDim2.new(0, 0, 0, 0)
ProgressBarFill.BackgroundColor3 = Color3.fromRGB(100, 50, 200)
ProgressBarFill.BorderSizePixel = 0
ProgressBarFill.ZIndex = 104
ProgressBarFill.Parent = ProgressBarBG

local ProgressFillCorner = Instance.new("UICorner")
ProgressFillCorner.CornerRadius = UDim.new(1, 0)
ProgressFillCorner.Parent = ProgressBarFill

local ProgressFillGradient = Instance.new("UIGradient")
ProgressFillGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 50, 80)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(150, 50, 220)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(50, 150, 255))
})
ProgressFillGradient.Parent = ProgressBarFill

-- Progress percentage text
local ProgressPercent = Instance.new("TextLabel")
ProgressPercent.Name = "ProgressPercent"
ProgressPercent.Size = UDim2.new(0, 100, 0, 25)
ProgressPercent.Position = UDim2.new(0.5, -50, 0.85, 0)
ProgressPercent.BackgroundTransparency = 1
ProgressPercent.Text = "0%"
ProgressPercent.TextColor3 = Color3.fromRGB(200, 200, 255)
ProgressPercent.TextSize = 16
ProgressPercent.Font = Enum.Font.GothamBold
ProgressPercent.ZIndex = 103
ProgressPercent.Parent = LoadingFrame

-- Decorative corner elements
local corners = {
    {UDim2.new(0.05, 0, 0.05, 0), "◇"},
    {UDim2.new(0.92, 0, 0.05, 0), "◇"},
    {UDim2.new(0.05, 0, 0.9, 0), "◇"},
    {UDim2.new(0.92, 0, 0.9, 0), "◇"},
}

for _, cornerData in ipairs(corners) do
    local corner = Instance.new("TextLabel")
    corner.Size = UDim2.new(0, 40, 0, 40)
    corner.Position = cornerData[1]
    corner.BackgroundTransparency = 1
    corner.Text = cornerData[2]
    corner.TextColor3 = Color3.fromRGB(100, 60, 160)
    corner.TextSize = 30
    corner.Font = Enum.Font.GothamBold
    corner.ZIndex = 103
    corner.Parent = LoadingFrame
    
    -- Pulse animation
    spawn(function()
        while corner.Parent do
            TweenService:Create(corner, TweenInfo.new(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
                TextTransparency = 0.7
            }):Play()
            wait(1.5)
            TweenService:Create(corner, TweenInfo.new(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
                TextTransparency = 0
            }):Play()
            wait(1.5)
        end
    end)
end

-- Side decorative symbols
local sideSymbols = {
    {UDim2.new(0.15, 0, 0.45, 0), "✦"},
    {UDim2.new(0.83, 0, 0.45, 0), "✦"},
    {UDim2.new(0.5, -10, 0.62, 0), "◆"},
}

for _, symData in ipairs(sideSymbols) do
    local sym = Instance.new("TextLabel")
    sym.Size = UDim2.new(0, 30, 0, 30)
    sym.Position = symData[1]
    sym.BackgroundTransparency = 1
    sym.Text = symData[2]
    sym.TextColor3 = Color3.fromRGB(180, 80, 120)
    sym.TextSize = 20
    sym.Font = Enum.Font.GothamBold
    sym.ZIndex = 103
    sym.TextTransparency = 0.3
    sym.Parent = LoadingFrame
end

-- Powered by RGC text at bottom
local PoweredText = Instance.new("TextLabel")
PoweredText.Name = "PoweredBy"
PoweredText.Size = UDim2.new(0, 300, 0, 25)
PoweredText.Position = UDim2.new(0.5, -150, 0.92, 0)
PoweredText.BackgroundTransparency = 1
PoweredText.Text = "Powered by: RGC"
PoweredText.TextColor3 = Color3.fromRGB(120, 120, 160)
PoweredText.TextSize = 14
PoweredText.Font = Enum.Font.Gotham
PoweredText.ZIndex = 103
PoweredText.Parent = LoadingFrame

-- ============================================================
-- LOADING ANIMATION (3 seconds)
-- ============================================================

local loadingStatuses = {
    {0.0, "Initializing modules..."},
    {0.15, "Loading Universal Framework..."},
    {0.30, "Establishing RGC Connection..."},
    {0.45, "Authenticating user..."},
    {0.60, "Loading Farm modules..."},
    {0.75, "Loading Combat modules..."},
    {0.85, "Finalizing configuration..."},
    {0.95, "RGC Server Connection Established | System Integrity Check Complete"},
    {1.0, "Ready!"},
}

spawn(function()
    local totalTime = 3
    local startTime = tick()
    
    while tick() - startTime < totalTime do
        local elapsed = tick() - startTime
        local progress = math.clamp(elapsed / totalTime, 0, 1)
        
        -- Update progress bar
        TweenService:Create(ProgressBarFill, TweenInfo.new(0.1), {
            Size = UDim2.new(progress, 0, 1, 0)
        }):Play()
        
        -- Update percentage
        ProgressPercent.Text = math.floor(progress * 100) .. "%"
        
        -- Update status text
        for i = #loadingStatuses, 1, -1 do
            if progress >= loadingStatuses[i][1] then
                StatusText.Text = loadingStatuses[i][2]
                break
            end
        end
        
        -- Animate loading dots
        local dots = string.rep(".", (math.floor(tick() * 3) % 3) + 1)
        if progress < 1 then
            LoadingText.Text = "LOADING" .. dots .. " [Establishing RGC Connection]"
        end
        
        RunService.Heartbeat:Wait()
    end
    
    -- Final state
    ProgressBarFill.Size = UDim2.new(1, 0, 1, 0)
    ProgressPercent.Text = "100%"
    LoadingText.Text = "✦ LOADED SUCCESSFULLY ✦"
    LoadingText.TextColor3 = Color3.fromRGB(100, 255, 150)
    StatusText.Text = "RGC Server Connection Established | System Integrity Check Complete"
    
    wait(0.5)
    
    -- Fade out loading screen
    local fadeOut = TweenService:Create(LoadingFrame, TweenInfo.new(0.8, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        BackgroundTransparency = 1
    })
    
    -- Fade all children
    for _, child in ipairs(LoadingFrame:GetDescendants()) do
        if child:IsA("TextLabel") then
            TweenService:Create(child, TweenInfo.new(0.8), {TextTransparency = 1}):Play()
        elseif child:IsA("Frame") then
            TweenService:Create(child, TweenInfo.new(0.8), {BackgroundTransparency = 1}):Play()
        elseif child:IsA("UIStroke") then
            TweenService:Create(child, TweenInfo.new(0.8), {Transparency = 1}):Play()
        end
    end
    
    fadeOut:Play()
    fadeOut.Completed:Wait()
    
    LoadingFrame:Destroy()
    
    -- Show the toggle button
    ToggleButton.Visible = true
    TweenService:Create(ToggleButton, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
        Size = UDim2.new(0, 50, 0, 50),
        BackgroundTransparency = 0
    }):Play()
end)

-- ============================================================
-- TOGGLE BUTTON (Small Circle)
-- ============================================================

local ToggleButton = Instance.new("TextButton")
ToggleButton.Name = "ToggleButton"
ToggleButton.Size = UDim2.new(0, 50, 0, 50)
ToggleButton.Position = UDim2.new(0, 15, 0.5, -25)
ToggleButton.BackgroundColor3 = Color3.fromRGB(80, 30, 150)
ToggleButton.Text = "U"
ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleButton.TextSize = 22
ToggleButton.Font = Enum.Font.GothamBold
ToggleButton.BorderSizePixel = 0
ToggleButton.ZIndex = 50
ToggleButton.Visible = false
ToggleButton.BackgroundTransparency = 1
ToggleButton.Parent = ScreenGui

local ToggleCorner = Instance.new("UICorner")
ToggleCorner.CornerRadius = UDim.new(1, 0)
ToggleCorner.Parent = ToggleButton

local ToggleStroke = Instance.new("UIStroke")
ToggleStroke.Color = Color3.fromRGB(150, 80, 255)
ToggleStroke.Thickness = 2
ToggleStroke.Parent = ToggleButton

-- Toggle button glow animation
spawn(function()
    while ToggleButton.Parent do
        TweenService:Create(ToggleStroke, TweenInfo.new(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
            Color = Color3.fromRGB(255, 80, 120)
        }):Play()
        wait(1.5)
        TweenService:Create(ToggleStroke, TweenInfo.new(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
            Color = Color3.fromRGB(80, 150, 255)
        }):Play()
        wait(1.5)
    end
end)

-- Toggle button drag
local toggleDragging = false
local toggleDragStart, toggleStartPos

ToggleButton.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        toggleDragging = true
        toggleDragStart = input.Position
        toggleStartPos = ToggleButton.Position
        
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                toggleDragging = false
            end
        end)
    end
end)

ToggleButton.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        if toggleDragging then
            local delta = input.Position - toggleDragStart
            ToggleButton.Position = UDim2.new(
                toggleStartPos.X.Scale, toggleStartPos.X.Offset + delta.X,
                toggleStartPos.Y.Scale, toggleStartPos.Y.Offset + delta.Y
            )
        end
    end
end)

-- ============================================================
-- MAIN UI FRAME
-- ============================================================

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 750, 0, 500)
MainFrame.Position = UDim2.new(0.5, -375, 0.5, -250)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
MainFrame.BorderSizePixel = 0
MainFrame.Visible = false
MainFrame.ZIndex = 10
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 12)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.fromRGB(80, 40, 150)
MainStroke.Thickness = 2
MainStroke.Transparency = 0.3
MainStroke.Parent = MainFrame

-- Animate main frame border
spawn(function()
    while MainFrame.Parent do
        TweenService:Create(MainStroke, TweenInfo.new(2, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
            Color = Color3.fromRGB(255, 60, 100)
        }):Play()
        wait(2)
        TweenService:Create(MainStroke, TweenInfo.new(2, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
            Color = Color3.fromRGB(60, 100, 255)
        }):Play()
        wait(2)
    end
end)

-- Make main frame draggable
local dragging, dragInput, dragStart, startPos

local TopBar = Instance.new("Frame")
TopBar.Name = "TopBar"
TopBar.Size = UDim2.new(1, 0, 0, 5)
TopBar.Position = UDim2.new(0, 0, 0, 0)
TopBar.BackgroundTransparency = 1
TopBar.ZIndex = 11
TopBar.Parent = MainFrame

MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
        
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

MainFrame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(
            startPos.X.Scale, startPos.X.Offset + delta.X,
            startPos.Y.Scale, startPos.Y.Offset + delta.Y
        )
    end
end)

-- ============================================================
-- HEADER SECTION (Left side - Title area)
-- ============================================================

local LeftPanel = Instance.new("Frame")
LeftPanel.Name = "LeftPanel"
LeftPanel.Size = UDim2.new(0, 220, 1, 0)
LeftPanel.Position = UDim2.new(0, 0, 0, 0)
LeftPanel.BackgroundColor3 = Color3.fromRGB(12, 12, 22)
LeftPanel.BorderSizePixel = 0
LeftPanel.ZIndex = 11
LeftPanel.Parent = MainFrame

local LeftPanelCorner = Instance.new("UICorner")
LeftPanelCorner.CornerRadius = UDim.new(0, 12)
LeftPanelCorner.Parent = LeftPanel

-- Fix corner overlap on right side of left panel
local LeftPanelFix = Instance.new("Frame")
LeftPanelFix.Size = UDim2.new(0, 15, 1, 0)
LeftPanelFix.Position = UDim2.new(1, -15, 0, 0)
LeftPanelFix.BackgroundColor3 = Color3.fromRGB(12, 12, 22)
LeftPanelFix.BorderSizePixel = 0
LeftPanelFix.ZIndex = 11
LeftPanelFix.Parent = LeftPanel

-- Divider line
local Divider = Instance.new("Frame")
Divider.Size = UDim2.new(0, 2, 1, -20)
Divider.Position = UDim2.new(1, -1, 0, 10)
Divider.BackgroundColor3 = Color3.fromRGB(60, 30, 120)
Divider.BorderSizePixel = 0
Divider.ZIndex = 12
Divider.Parent = LeftPanel

-- "Universal" title in left panel
local MainTitle = Instance.new("TextLabel")
MainTitle.Name = "MainTitle"
MainTitle.Size = UDim2.new(1, 0, 0, 45)
MainTitle.Position = UDim2.new(0, 0, 0, 15)
MainTitle.BackgroundTransparency = 1
MainTitle.Text = "Universal"
MainTitle.TextColor3 = Color3.fromRGB(200, 100, 180)
MainTitle.TextSize = 32
MainTitle.Font = Enum.Font.GothamBold
MainTitle.ZIndex = 12
MainTitle.Parent = LeftPanel

local MainTitleGradient = Instance.new("UIGradient")
MainTitleGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 80, 120)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(180, 100, 255)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(100, 150, 255))
})
MainTitleGradient.Parent = MainTitle

-- Animate title
spawn(function()
    local r = 0
    while MainTitle.Parent do
        r = (r + 0.5) % 360
        MainTitleGradient.Rotation = r
        RunService.Heartbeat:Wait()
    end
end)

-- "By:DevNameGelo"
local ByLabel = Instance.new("TextLabel")
ByLabel.Size = UDim2.new(1, 0, 0, 20)
ByLabel.Position = UDim2.new(0, 0, 0, 55)
ByLabel.BackgroundTransparency = 1
ByLabel.Text = "By:DevNameGelo"
ByLabel.TextColor3 = Color3.fromRGB(140, 120, 200)
ByLabel.TextSize = 14
ByLabel.Font = Enum.Font.Gotham
ByLabel.ZIndex = 12
ByLabel.Parent = LeftPanel

-- "Powered by:RGC"
local PoweredLabel = Instance.new("TextLabel")
PoweredLabel.Size = UDim2.new(1, 0, 0, 30)
PoweredLabel.Position = UDim2.new(0, 0, 0, 78)
PoweredLabel.BackgroundTransparency = 1
PoweredLabel.Text = "Powered by:RGC"
PoweredLabel.TextColor3 = Color3.fromRGB(180, 150, 230)
PoweredLabel.TextSize = 16
PoweredLabel.Font = Enum.Font.GothamBold
PoweredLabel.TextXAlignment = Enum.TextXAlignment.Center
PoweredLabel.ZIndex = 12
PoweredLabel.Parent = LeftPanel

-- ============================================================
-- TAB BUTTONS (Left Panel)
-- ============================================================

local tabButtons = {}
local tabFrames = {}
local currentTab = "Farm"

local tabData = {
    {name = "Farm", icon = "🌿", order = 1},
    {name = "Combat", icon = "⚔", order = 2},
    {name = "Player", icon = "👤", order = 3},
    {name = "Teleport", icon = "✨", order = 4},
    {name = "Misc", icon = "⚙", order = 5},
    {name = "Settings", icon = "🔧", order = 6},
}

local TabContainer = Instance.new("ScrollingFrame")
TabContainer.Name = "TabContainer"
TabContainer.Size = UDim2.new(1, -20, 0, 340)
TabContainer.Position = UDim2.new(0, 10, 0, 115)
TabContainer.BackgroundTransparency = 1
TabContainer.ScrollBarThickness = 0
TabContainer.CanvasSize = UDim2.new(0, 0, 0, #tabData * 75)
TabContainer.ZIndex = 12
TabContainer.Parent = LeftPanel

local TabListLayout = Instance.new("UIListLayout")
TabListLayout.SortOrder = Enum.SortOrder.LayoutOrder
TabListLayout.Padding = UDim.new(0, 8)
TabListLayout.Parent = TabContainer

for i, tab in ipairs(tabData) do
    local tabBtn = Instance.new("TextButton")
    tabBtn.Name = "Tab_" .. tab.name
    tabBtn.Size = UDim2.new(1, 0, 0, 65)
    tabBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 45)
    tabBtn.BorderSizePixel = 0
    tabBtn.Text = ""
    tabBtn.LayoutOrder = tab.order
    tabBtn.ZIndex = 13
    tabBtn.Parent = TabContainer
    
    local tabBtnCorner = Instance.new("UICorner")
    tabBtnCorner.CornerRadius = UDim.new(0, 10)
    tabBtnCorner.Parent = tabBtn
    
    local tabBtnStroke = Instance.new("UIStroke")
    tabBtnStroke.Color = Color3.fromRGB(50, 30, 100)
    tabBtnStroke.Thickness = 1.5
    tabBtnStroke.Parent = tabBtn
    
    -- Tab icon
    local tabIcon = Instance.new("TextLabel")
    tabIcon.Size = UDim2.new(1, 0, 0, 35)
    tabIcon.Position = UDim2.new(0, 0, 0, 5)
    tabIcon.BackgroundTransparency = 1
    tabIcon.Text = tab.icon
    tabIcon.TextColor3 = Color3.fromRGB(180, 150, 230)
    tabIcon.TextSize = 28
    tabIcon.Font = Enum.Font.GothamBold
    tabIcon.ZIndex = 14
    tabIcon.Parent = tabBtn
    
    -- Tab name
    local tabName = Instance.new("TextLabel")
    tabName.Size = UDim2.new(1, 0, 0, 20)
    tabName.Position = UDim2.new(0, 0, 0, 40)
    tabName.BackgroundTransparency = 1
    tabName.Text = tab.name
    tabName.TextColor3 = Color3.fromRGB(150, 120, 200)
    tabName.TextSize = 14
    tabName.Font = Enum.Font.GothamBold
    tabName.ZIndex = 14
    tabName.Parent = tabBtn
    
    tabButtons[tab.name] = {button = tabBtn, stroke = tabBtnStroke, icon = tabIcon, nameLabel = tabName}
    
    tabBtn.MouseButton1Click:Connect(function()
        -- Switch tab
        currentTab = tab.name
        
        for tName, tData in pairs(tabButtons) do
            if tName == tab.name then
                TweenService:Create(tData.button, TweenInfo.new(0.3), {BackgroundColor3 = Color3.fromRGB(40, 20, 80)}):Play()
                TweenService:Create(tData.stroke, TweenInfo.new(0.3), {Color = Color3.fromRGB(255, 80, 120)}):Play()
                TweenService:Create(tData.icon, TweenInfo.new(0.3), {TextColor3 = Color3.fromRGB(255, 120, 180)}):Play()
                TweenService:Create(tData.nameLabel, TweenInfo.new(0.3), {TextColor3 = Color3.fromRGB(255, 100, 150)}):Play()
            else
                TweenService:Create(tData.button, TweenInfo.new(0.3), {BackgroundColor3 = Color3.fromRGB(25, 25, 45)}):Play()
                TweenService:Create(tData.stroke, TweenInfo.new(0.3), {Color = Color3.fromRGB(50, 30, 100)}):Play()
                TweenService:Create(tData.icon, TweenInfo.new(0.3), {TextColor3 = Color3.fromRGB(180, 150, 230)}):Play()
                TweenService:Create(tData.nameLabel, TweenInfo.new(0.3), {TextColor3 = Color3.fromRGB(150, 120, 200)}):Play()
            end
        end
        
        for tName, tFrame in pairs(tabFrames) do
            tFrame.Visible = (tName == tab.name)
        end
    end)
    
    -- Hover effects
    tabBtn.MouseEnter:Connect(function()
        if currentTab ~= tab.name then
            TweenService:Create(tabBtn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(35, 25, 60)}):Play()
        end
    end)
    
    tabBtn.MouseLeave:Connect(function()
        if currentTab ~= tab.name then
            TweenService:Create(tabBtn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(25, 25, 45)}):Play()
        end
    end)
end

-- Set Farm as default selected
if tabButtons["Farm"] then
    tabButtons["Farm"].button.BackgroundColor3 = Color3.fromRGB(40, 20, 80)
    tabButtons["Farm"].stroke.Color = Color3.fromRGB(255, 80, 120)
    tabButtons["Farm"].icon.TextColor3 = Color3.fromRGB(255, 120, 180)
    tabButtons["Farm"].nameLabel.TextColor3 = Color3.fromRGB(255, 100, 150)
end

-- ============================================================
-- RIGHT PANEL (Content Area)
-- ============================================================

local RightPanel = Instance.new("Frame")
RightPanel.Name = "RightPanel"
RightPanel.Size = UDim2.new(1, -225, 1, -10)
RightPanel.Position = UDim2.new(0, 225, 0, 5)
RightPanel.BackgroundTransparency = 1
RightPanel.ZIndex = 11
RightPanel.Parent = MainFrame

-- Close button
local CloseBtn = Instance.new("TextButton")
CloseBtn.Name = "CloseBtn"
CloseBtn.Size = UDim2.new(0, 35, 0, 35)
CloseBtn.Position = UDim2.new(1, -45, 0, 5)
CloseBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
CloseBtn.BackgroundTransparency = 0.5
CloseBtn.Text = "✕"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.TextSize = 18
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.BorderSizePixel = 0
CloseBtn.ZIndex = 15
CloseBtn.Parent = RightPanel

local CloseBtnCorner = Instance.new("UICorner")
CloseBtnCorner.CornerRadius = UDim.new(0, 8)
CloseBtnCorner.Parent = CloseBtn

CloseBtn.MouseEnter:Connect(function()
    TweenService:Create(CloseBtn, TweenInfo.new(0.2), {BackgroundTransparency = 0, BackgroundColor3 = Color3.fromRGB(255, 60, 60)}):Play()
end)

CloseBtn.MouseLeave:Connect(function()
    TweenService:Create(CloseBtn, TweenInfo.new(0.2), {BackgroundTransparency = 0.5, BackgroundColor3 = Color3.fromRGB(200, 50, 50)}):Play()
end)

-- Minimize button
local MinBtn = Instance.new("TextButton")
MinBtn.Name = "MinBtn"
MinBtn.Size = UDim2.new(0, 35, 0, 35)
MinBtn.Position = UDim2.new(1, -85, 0, 5)
MinBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 120)
MinBtn.BackgroundTransparency = 0.5
MinBtn.Text = "—"
MinBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
MinBtn.TextSize = 18
MinBtn.Font = Enum.Font.GothamBold
MinBtn.BorderSizePixel = 0
MinBtn.ZIndex = 15
MinBtn.Parent = RightPanel

local MinBtnCorner = Instance.new("UICorner")
MinBtnCorner.CornerRadius = UDim.new(0, 8)
MinBtnCorner.Parent = MinBtn

MinBtn.MouseEnter:Connect(function()
    TweenService:Create(MinBtn, TweenInfo.new(0.2), {BackgroundTransparency = 0}):Play()
end)

MinBtn.MouseLeave:Connect(function()
    TweenService:Create(MinBtn, TweenInfo.new(0.2), {BackgroundTransparency = 0.5}):Play()
end)

-- ============================================================
-- UTILITY FUNCTIONS FOR CREATING UI ELEMENTS
-- ============================================================

local function createSectionHeader(parent, text, position)
    local header = Instance.new("TextLabel")
    header.Size = UDim2.new(1, -20, 0, 30)
    header.Position = position
    header.BackgroundTransparency = 1
    header.Text = "— " .. text .. " —"
    header.TextColor3 = Color3.fromRGB(255, 120, 160)
    header.TextSize = 20
    header.Font = Enum.Font.GothamBold
    header.TextXAlignment = Enum.TextXAlignment.Left
    header.ZIndex = 13
    header.Parent = parent
    
    -- Underline
    local underline = Instance.new("Frame")
    underline.Size = UDim2.new(1, 0, 0, 1)
    underline.Position = UDim2.new(0, 0, 1, 2)
    underline.BackgroundColor3 = Color3.fromRGB(255, 80, 100)
    underline.BackgroundTransparency = 0.5
    underline.BorderSizePixel = 0
    underline.ZIndex = 13
    underline.Parent = header
    
    return header
end

local function createToggle(parent, text, position, default, callback)
    local toggleFrame = Instance.new("Frame")
    toggleFrame.Size = UDim2.new(1, -20, 0, 35)
    toggleFrame.Position = position
    toggleFrame.BackgroundTransparency = 1
    toggleFrame.ZIndex = 13
    toggleFrame.Parent = parent
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.7, 0, 1, 0)
    label.Position = UDim2.new(0, 0, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = Color3.fromRGB(220, 220, 240)
    label.TextSize = 16
    label.Font = Enum.Font.GothamBold
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.ZIndex = 14
    label.Parent = toggleFrame
    
    local toggleBG = Instance.new("TextButton")
    toggleBG.Size = UDim2.new(0, 55, 0, 28)
    toggleBG.Position = UDim2.new(1, -60, 0.5, -14)
    toggleBG.BackgroundColor3 = default and Color3.fromRGB(80, 40, 180) or Color3.fromRGB(50, 50, 70)
    toggleBG.BorderSizePixel = 0
    toggleBG.Text = ""
    toggleBG.ZIndex = 14
    toggleBG.Parent = toggleFrame
    
    local toggleBGCorner = Instance.new("UICorner")
    toggleBGCorner.CornerRadius = UDim.new(1, 0)
    toggleBGCorner.Parent = toggleBG
    
    local toggleCircle = Instance.new("Frame")
    toggleCircle.Size = UDim2.new(0, 22, 0, 22)
    toggleCircle.Position = default and UDim2.new(1, -25, 0.5, -11) or UDim2.new(0, 3, 0.5, -11)
    toggleCircle.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    toggleCircle.BorderSizePixel = 0
    toggleCircle.ZIndex = 15
    toggleCircle.Parent = toggleBG
    
    local toggleCircleCorner = Instance.new("UICorner")
    toggleCircleCorner.CornerRadius = UDim.new(1, 0)
    toggleCircleCorner.Parent = toggleCircle
    
    -- ON/OFF label
    local onOffLabel = Instance.new("TextLabel")
    onOffLabel.Size = UDim2.new(0, 30, 0, 28)
    onOffLabel.Position = UDim2.new(1, -95, 0.5, -14)
    onOffLabel.BackgroundTransparency = 1
    onOffLabel.Text = default and "ON" or "OFF"
    onOffLabel.TextColor3 = default and Color3.fromRGB(100, 255, 150) or Color3.fromRGB(255, 100, 100)
    onOffLabel.TextSize = 12
    onOffLabel.Font = Enum.Font.GothamBold
    onOffLabel.ZIndex = 14
    onOffLabel.Parent = toggleFrame
    
    local enabled = default or false
    
    toggleBG.MouseButton1Click:Connect(function()
        enabled = not enabled
        
        if enabled then
            TweenService:Create(toggleCircle, TweenInfo.new(0.25, Enum.EasingStyle.Quad), {
                Position = UDim2.new(1, -25, 0.5, -11)
            }):Play()
            TweenService:Create(toggleBG, TweenInfo.new(0.25), {
                BackgroundColor3 = Color3.fromRGB(80, 40, 180)
            }):Play()
            onOffLabel.Text = "ON"
            onOffLabel.TextColor3 = Color3.fromRGB(100, 255, 150)
        else
            TweenService:Create(toggleCircle, TweenInfo.new(0.25, Enum.EasingStyle.Quad), {
                Position = UDim2.new(0, 3, 0.5, -11)
            }):Play()
            TweenService:Create(toggleBG, TweenInfo.new(0.25), {
                BackgroundColor3 = Color3.fromRGB(50, 50, 70)
            }):Play()
            onOffLabel.Text = "OFF"
            onOffLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
        end
        
        if callback then callback(enabled) end
    end)
    
    return toggleFrame, function() return enabled end
end

local function createSlider(parent, text, position, minVal, maxVal, default, callback)
    local sliderFrame = Instance.new("Frame")
    sliderFrame.Size = UDim2.new(1, -20, 0, 40)
    sliderFrame.Position = position
    sliderFrame.BackgroundTransparency = 1
    sliderFrame.ZIndex = 13
    sliderFrame.Parent = parent
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0, 150, 1, 0)
    label.Position = UDim2.new(0, 0, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = Color3.fromRGB(220, 220, 240)
    label.TextSize = 16
    label.Font = Enum.Font.GothamBold
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.ZIndex = 14
    label.Parent = sliderFrame
    
    local sliderBG = Instance.new("Frame")
    sliderBG.Size = UDim2.new(0, 200, 0, 8)
    sliderBG.Position = UDim2.new(0, 160, 0.5, -4)
    sliderBG.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
    sliderBG.BorderSizePixel = 0
    sliderBG.ZIndex = 14
    sliderBG.Parent = sliderFrame
    
    local sliderBGCorner = Instance.new("UICorner")
    sliderBGCorner.CornerRadius = UDim.new(1, 0)
    sliderBGCorner.Parent = sliderBG
    
    local sliderFill = Instance.new("Frame")
    local fillPercent = (default - minVal) / (maxVal - minVal)
    sliderFill.Size = UDim2.new(fillPercent, 0, 1, 0)
    sliderFill.BackgroundColor3 = Color3.fromRGB(255, 60, 100)
    sliderFill.BorderSizePixel = 0
    sliderFill.ZIndex = 15
    sliderFill.Parent = sliderBG
    
    local sliderFillCorner = Instance.new("UICorner")
    sliderFillCorner.CornerRadius = UDim.new(1, 0)
    sliderFillCorner.Parent = sliderFill
    
    local sliderFillGradient = Instance.new("UIGradient")
    sliderFillGradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 50, 80)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(80, 120, 255))
    })
    sliderFillGradient.Parent = sliderFill
    
    -- Slider knob
    local knob = Instance.new("Frame")
    knob.Size = UDim2.new(0, 18, 0, 18)
    knob.Position = UDim2.new(fillPercent, -9, 0.5, -9)
    knob.BackgroundColor3 = Color3.fromRGB(120, 140, 255)
    knob.BorderSizePixel = 0
    knob.ZIndex = 16
    knob.Parent = sliderBG
    
    local knobCorner = Instance.new("UICorner")
    knobCorner.CornerRadius = UDim.new(1, 0)
    knobCorner.Parent = knob
    
    -- Value display
    local valueLabel = Instance.new("TextLabel")
    valueLabel.Size = UDim2.new(0, 60, 1, 0)
    valueLabel.Position = UDim2.new(0, 370, 0, 0)
    valueLabel.BackgroundTransparency = 1
    valueLabel.Text = tostring(default) .. "%"
    valueLabel.TextColor3 = Color3.fromRGB(200, 200, 255)
    valueLabel.TextSize = 16
    valueLabel.Font = Enum.Font.GothamBold
    valueLabel.ZIndex = 14
    valueLabel.Parent = sliderFrame
    
    -- Slider interaction
    local sliderDragging = false
    
    local function updateSlider(input)
        local absPos = sliderBG.AbsolutePosition.X
        local absSize = sliderBG.AbsoluteSize.X
        local mouseX = input.Position.X
        local relative = math.clamp((mouseX - absPos) / absSize, 0, 1)
        
        sliderFill.Size = UDim2.new(relative, 0, 1, 0)
        knob.Position = UDim2.new(relative, -9, 0.5, -9)
        
        local value = math.floor(minVal + (maxVal - minVal) * relative)
        valueLabel.Text = tostring(value) .. "%"
        
        if callback then callback(value) end
    end
    
    sliderBG.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            sliderDragging = true
            updateSlider(input)
        end
    end)
    
    UserInputService.InputChanged:Connect(function(input)
        if sliderDragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            updateSlider(input)
        end
    end)
    
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            sliderDragging = false
        end
    end)
    
    return sliderFrame
end

local function createButton(parent, text, position, size, callback)
    local btn = Instance.new("TextButton")
    btn.Size = size or UDim2.new(0, 200, 0, 38)
    btn.Position = position
    btn.BackgroundColor3 = Color3.fromRGB(30, 30, 60)
    btn.BorderSizePixel = 0
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(200, 200, 255)
    btn.TextSize = 15
    btn.Font = Enum.Font.GothamBold
    btn.ZIndex = 14
    btn.Parent = parent
    
    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 8)
    btnCorner.Parent = btn
    
    local btnStroke = Instance.new("UIStroke")
    btnStroke.Color = Color3.fromRGB(80, 60, 180)
    btnStroke.Thickness = 1.5
    btnStroke.Parent = btn
    
    btn.MouseEnter:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(50, 40, 100)}):Play()
        TweenService:Create(btnStroke, TweenInfo.new(0.2), {Color = Color3.fromRGB(255, 80, 120)}):Play()
    end)
    
    btn.MouseLeave:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(30, 30, 60)}):Play()
        TweenService:Create(btnStroke, TweenInfo.new(0.2), {Color = Color3.fromRGB(80, 60, 180)}):Play()
    end)
    
    btn.MouseButton1Click:Connect(function()
        -- Click animation
        TweenService:Create(btn, TweenInfo.new(0.1), {BackgroundColor3 = Color3.fromRGB(80, 60, 160)}):Play()
        wait(0.1)
        TweenService:Create(btn, TweenInfo.new(0.1), {BackgroundColor3 = Color3.fromRGB(50, 40, 100)}):Play()
        if callback then callback() end
    end)
    
    return btn
end

local function createDropdown(parent, text, position, options, callback)
    local ddFrame = Instance.new("Frame")
    ddFrame.Size = UDim2.new(1, -20, 0, 35)
    ddFrame.Position = position
    ddFrame.BackgroundTransparency = 1
    ddFrame.ZIndex = 13
    ddFrame.ClipsDescendants = false
    ddFrame.Parent = parent
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.5, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = Color3.fromRGB(220, 220, 240)
    label.TextSize = 16
    label.Font = Enum.Font.GothamBold
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.ZIndex = 14
    label.Parent = ddFrame
    
    local dropBtn = Instance.new("TextButton")
    dropBtn.Size = UDim2.new(0, 180, 0, 30)
    dropBtn.Position = UDim2.new(1, -185, 0, 2)
    dropBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 55)
    dropBtn.BorderSizePixel = 0
    dropBtn.Text = options[1] or "Select"
    dropBtn.TextColor3 = Color3.fromRGB(200, 200, 220)
    dropBtn.TextSize = 14
    dropBtn.Font = Enum.Font.Gotham
    dropBtn.ZIndex = 15
    dropBtn.Parent = ddFrame
    
    local dropBtnCorner = Instance.new("UICorner")
    dropBtnCorner.CornerRadius = UDim.new(0, 6)
    dropBtnCorner.Parent = dropBtn
    
    -- Arrow
    local arrow = Instance.new("TextLabel")
    arrow.Size = UDim2.new(0, 20, 0, 30)
    arrow.Position = UDim2.new(1, -25, 0, 0)
    arrow.BackgroundTransparency = 1
    arrow.Text = "▼"
    arrow.TextColor3 = Color3.fromRGB(150, 150, 200)
    arrow.TextSize = 12
    arrow.Font = Enum.Font.GothamBold
    arrow.ZIndex = 16
    arrow.Parent = dropBtn
    
    -- Dropdown list
    local dropList = Instance.new("Frame")
    dropList.Size = UDim2.new(0, 180, 0, #options * 30)
    dropList.Position = UDim2.new(1, -185, 0, 35)
    dropList.BackgroundColor3 = Color3.fromRGB(30, 30, 50)
    dropList.BorderSizePixel = 0
    dropList.Visible = false
    dropList.ZIndex = 20
    dropList.Parent = ddFrame
    
    local dropListCorner = Instance.new("UICorner")
    dropListCorner.CornerRadius = UDim.new(0, 6)
    dropListCorner.Parent = dropList
    
    local dropListStroke = Instance.new("UIStroke")
    dropListStroke.Color = Color3.fromRGB(60, 40, 120)
    dropListStroke.Thickness = 1
    dropListStroke.Parent = dropList
    
    for j, option in ipairs(options) do
        local optBtn = Instance.new("TextButton")
        optBtn.Size = UDim2.new(1, -6, 0, 28)
        optBtn.Position = UDim2.new(0, 3, 0, (j - 1) * 30 + 1)
        optBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 55)
        optBtn.BackgroundTransparency = 0.5
        optBtn.BorderSizePixel = 0
        optBtn.Text = option
        optBtn.TextColor3 = Color3.fromRGB(200, 200, 220)
        optBtn.TextSize = 14
        optBtn.Font = Enum.Font.Gotham
        optBtn.ZIndex = 21
        optBtn.Parent = dropList
        
        local optBtnCorner = Instance.new("UICorner")
        optBtnCorner.CornerRadius = UDim.new(0, 4)
        optBtnCorner.Parent = optBtn
        
        optBtn.MouseEnter:Connect(function()
            TweenService:Create(optBtn, TweenInfo.new(0.15), {BackgroundTransparency = 0, BackgroundColor3 = Color3.fromRGB(60, 40, 120)}):Play()
        end)
        
        optBtn.MouseLeave:Connect(function()
            TweenService:Create(optBtn, TweenInfo.new(0.15), {BackgroundTransparency = 0.5, BackgroundColor3 = Color3.fromRGB(35, 35, 55)}):Play()
        end)
        
        optBtn.MouseButton1Click:Connect(function()
            dropBtn.Text = option
            dropList.Visible = false
            arrow.Text = "▼"
            if callback then callback(option) end
        end)
    end
    
    local dropOpen = false
    dropBtn.MouseButton1Click:Connect(function()
        dropOpen = not dropOpen
        dropList.Visible = dropOpen
        arrow.Text = dropOpen and "▲" or "▼"
    end)
    
    return ddFrame
end

-- ============================================================
-- TAB CONTENT: Tab Header
-- ============================================================

local TabHeader = Instance.new("TextLabel")
TabHeader.Name = "TabHeader"
TabHeader.Size = UDim2.new(0.7, 0, 0, 40)
TabHeader.Position = UDim2.new(0, 10, 0, 5)
TabHeader.BackgroundTransparency = 1
TabHeader.Text = "Tab: Farm"
TabHeader.TextColor3 = Color3.fromRGB(255, 100, 150)
TabHeader.TextSize = 24
TabHeader.Font = Enum.Font.GothamBold
TabHeader.TextXAlignment = Enum.TextXAlignment.Left
TabHeader.ZIndex = 13
TabHeader.Parent = RightPanel

local TabHeaderGradient = Instance.new("UIGradient")
TabHeaderGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 80, 120)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(120, 80, 255))
})
TabHeaderGradient.Parent = TabHeader

-- ============================================================
-- FARM TAB
-- ============================================================

local FarmTab = Instance.new("ScrollingFrame")
FarmTab.Name = "FarmTab"
FarmTab.Size = UDim2.new(1, -10, 1, -55)
FarmTab.Position = UDim2.new(0, 5, 0, 50)
FarmTab.BackgroundTransparency = 1
FarmTab.ScrollBarThickness = 4
FarmTab.ScrollBarImageColor3 = Color3.fromRGB(120, 80, 200)
FarmTab.CanvasSize = UDim2.new(0, 0, 0, 550)
FarmTab.Visible = true
FarmTab.ZIndex = 12
FarmTab.Parent = RightPanel
tabFrames["Farm"] = FarmTab

createSectionHeader(FarmTab, "Auto-Farm", UDim2.new(0, 10, 0, 5))

createToggle(FarmTab, "Enable Auto-Farm", UDim2.new(0, 10, 0, 40), false, function(state)
    print("Auto-Farm:", state)
end)

createDropdown(FarmTab, "Select Farm Target", UDim2.new(0, 10, 0, 80), {"Coin", "Gem", "XP", "All"}, function(option)
    print("Farm Target:", option)
end)

createButton(FarmTab, "Farm Money", UDim2.new(0, 10, 0, 130), UDim2.new(0, 200, 0, 38), function()
    print("Farming Money...")
end)

createButton(FarmTab, "Farm Experience", UDim2.new(0, 220, 0, 130), UDim2.new(0, 200, 0, 38), function()
    print("Farming Experience...")
end)

createToggle(FarmTab, "Auto-Collect", UDim2.new(0, 10, 0, 180), false, function(state)
    print("Auto-Collect:", state)
end)

createToggle(FarmTab, "Auto-Sell", UDim2.new(0, 10, 0, 220), false, function(state)
    print("Auto-Sell:", state)
end)

createSectionHeader(FarmTab, "Movement", UDim2.new(0, 10, 0, 270))

createToggle(FarmTab, "Fly", UDim2.new(0, 10, 0, 305), false, function(state)
    print("Fly:", state)
    if state then
        -- Basic fly implementation
        local character = LocalPlayer.Character
        if character then
            local humanoid = character:FindFirstChildOfClass("Humanoid")
            if humanoid then
                -- Simple fly toggle notification
                print("Fly mode activated!")
            end
        end
    end
end)

createToggle(FarmTab, "Infinite Jump", UDim2.new(0, 10, 0, 345), false, function(state)
    print("Infinite Jump:", state)
    if state then
        _G.InfiniteJump = true
        UserInputService.JumpRequest:Connect(function()
            if _G.InfiniteJump then
                local character = LocalPlayer.Character
                if character then
                    local humanoid = character:FindFirstChildOfClass("Humanoid")
                    if humanoid then
                        humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
                    end
                end
            end
        end)
    else
        _G.InfiniteJump = false
    end
end)

createSlider(FarmTab, "Walkspeed", UDim2.new(0, 10, 0, 390), 16, 200, 60, function(value)
    local character = LocalPlayer.Character
    if character then
        local humanoid = character:FindFirstChildOfClass("Humanoid")
        if humanoid then
            humanoid.WalkSpeed = value
        end
    end
end)

createSlider(FarmTab, "JumpPower", UDim2.new(0, 10, 0, 440), 50, 500, 50, function(value)
    local character = LocalPlayer.Character
    if character then
        local humanoid = character:FindFirstChildOfClass("Humanoid")
        if humanoid then
            humanoid.JumpPower = value
        end
    end
end)

createToggle(FarmTab, "No Clip", UDim2.new(0, 10, 0, 490), false, function(state)
    print("No Clip:", state)
    _G.NoClip = state
    if state then
        spawn(function()
            while _G.NoClip do
                local character = LocalPlayer.Character
                if character then
                    for _, part in ipairs(character:GetDescendants()) do
                        if part:IsA("BasePart") then
                            part.CanCollide = false
                        end
                    end
                end
                RunService.Stepped:Wait()
            end
        end)
    end
end)

-- ============================================================
-- COMBAT TAB
-- ============================================================

local CombatTab = Instance.new("ScrollingFrame")
CombatTab.Name = "CombatTab"
CombatTab.Size = UDim2.new(1, -10, 1, -55)
CombatTab.Position = UDim2.new(0, 5, 0, 50)
CombatTab.BackgroundTransparency = 1
CombatTab.ScrollBarThickness = 4
CombatTab.ScrollBarImageColor3 = Color3.fromRGB(120, 80, 200)
CombatTab.CanvasSize = UDim2.new(0, 0, 0, 450)
CombatTab.Visible = false
CombatTab.ZIndex = 12
CombatTab.Parent = RightPanel
tabFrames["Combat"] = CombatTab

createSectionHeader(CombatTab, "Combat Features", UDim2.new(0, 10, 0, 5))

createToggle(CombatTab, "Auto-Attack", UDim2.new(0, 10, 0, 40), false, function(state)
    print("Auto-Attack:", state)
end)

createToggle(CombatTab, "Kill Aura", UDim2.new(0, 10, 0, 80), false, function(state)
    print("Kill Aura:", state)
end)

createSlider(CombatTab, "Attack Range", UDim2.new(0, 10, 0, 120), 10, 100, 30, function(value)
    print("Attack Range:", value)
end)

createToggle(CombatTab, "Auto-Parry", UDim2.new(0, 10, 0, 170), false, function(state)
    print("Auto-Parry:", state)
end)

createToggle(CombatTab, "Auto-Dodge", UDim2.new(0, 10, 0, 210), false, function(state)
    print("Auto-Dodge:", state)
end)

createSectionHeader(CombatTab, "Boss Features", UDim2.new(0, 10, 0, 260))

createToggle(CombatTab, "Auto-Boss", UDim2.new(0, 10, 0, 295), false, function(state)
    print("Auto-Boss:", state)
end)

createDropdown(CombatTab, "Select Boss", UDim2.new(0, 10, 0, 335), {"Boss 1", "Boss 2", "Boss 3", "All Bosses"}, function(option)
    print("Selected Boss:", option)
end)

createToggle(CombatTab, "Auto-Use Skills", UDim2.new(0, 10, 0, 385), false, function(state)
    print("Auto-Use Skills:", state)
end)

-- ============================================================
-- PLAYER TAB
-- ============================================================

local PlayerTab = Instance.new("ScrollingFrame")
PlayerTab.Name = "PlayerTab"
PlayerTab.Size = UDim2.new(1, -10, 1, -55)
PlayerTab.Position = UDim2.new(0, 5, 0, 50)
PlayerTab.BackgroundTransparency = 1
PlayerTab.ScrollBarThickness = 4
PlayerTab.ScrollBarImageColor3 = Color3.fromRGB(120, 80, 200)
PlayerTab.CanvasSize = UDim2.new(0, 0, 0, 500)
PlayerTab.Visible = false
PlayerTab.ZIndex = 12
PlayerTab.Parent = RightPanel
tabFrames["Player"] = PlayerTab

createSectionHeader(PlayerTab, "Character", UDim2.new(0, 10, 0, 5))

createToggle(PlayerTab, "God Mode", UDim2.new(0, 10, 0, 40), false, function(state)
    print("God Mode:", state)
end)

createToggle(PlayerTab, "Invisible", UDim2.new(0, 10, 0, 80), false, function(state)
    print("Invisible:", state)
end)

createSlider(PlayerTab, "Size Scale", UDim2.new(0, 10, 0, 120), 50, 300, 100, function(value)
    print("Size:", value)
end)

createSectionHeader(PlayerTab, "Visual", UDim2.new(0, 10, 0, 175))

createToggle(PlayerTab, "ESP Players", UDim2.new(0, 10, 0, 210), false, function(state)
    print("ESP:", state)
end)

createToggle(PlayerTab, "ESP NPCs", UDim2.new(0, 10, 0, 250), false, function(state)
    print("ESP NPCs:", state)
end)

createToggle(PlayerTab, "Fullbright", UDim2.new(0, 10, 0, 290), false, function(state)
    print("Fullbright:", state)
    if state then
        local lighting = game:GetService("Lighting")
        lighting.Brightness = 2
        lighting.ClockTime = 14
        lighting.FogEnd = 100000
        lighting.GlobalShadows = false
    else
        local lighting = game:GetService("Lighting")
        lighting.Brightness = 1
        lighting.GlobalShadows = true
    end
end)

createSlider(PlayerTab, "FOV", UDim2.new(0, 10, 0, 340), 70, 120, 70, function(value)
    workspace.CurrentCamera.FieldOfView = value
end)

createToggle(PlayerTab, "Anti-AFK", UDim2.new(0, 10, 0, 390), true, function(state)
    print("Anti-AFK:", state)
end)

-- ============================================================
-- TELEPORT TAB
-- ============================================================

local TeleportTab = Instance.new("ScrollingFrame")
TeleportTab.Name = "TeleportTab"
TeleportTab.Size = UDim2.new(1, -10, 1, -55)
TeleportTab.Position = UDim2.new(0, 5, 0, 50)
TeleportTab.BackgroundTransparency = 1
TeleportTab.ScrollBarThickness = 4
TeleportTab.ScrollBarImageColor3 = Color3.fromRGB(120, 80, 200)
TeleportTab.CanvasSize = UDim2.new(0, 0, 0, 400)
TeleportTab.Visible = false
TeleportTab.ZIndex = 12
TeleportTab.Parent = RightPanel
tabFrames["Teleport"] = TeleportTab

createSectionHeader(TeleportTab, "Teleport Options", UDim2.new(0, 10, 0, 5))

createDropdown(TeleportTab, "Teleport To Player", UDim2.new(0, 10, 0, 40), {"Select Player"}, function(option)
    print("Teleport to:", option)
end)

createButton(TeleportTab, "TP to Spawn", UDim2.new(0, 10, 0, 90), UDim2.new(0, 200, 0, 38), function()
    local character = LocalPlayer.Character
    if character and character:FindFirstChild("HumanoidRootPart") then
        character.HumanoidRootPart.CFrame = CFrame.new(0, 10, 0)
    end
end)

createButton(TeleportTab, "TP to Waypoint", UDim2.new(0, 220, 0, 90), UDim2.new(0, 200, 0, 38), function()
    print("TP to Waypoint")
end)

createSectionHeader(TeleportTab, "Save Positions", UDim2.new(0, 10, 0, 145))

createButton(TeleportTab, "Save Position 1", UDim2.new(0, 10, 0, 185), UDim2.new(0, 200, 0, 38), function()
    local character = LocalPlayer.Character
    if character and character:FindFirstChild("HumanoidRootPart") then
        _G.SavedPos1 = character.HumanoidRootPart.CFrame
        print("Position 1 saved!")
    end
end)

createButton(TeleportTab, "Load Position 1", UDim2.new(0, 220, 0, 185), UDim2.new(0, 200, 0, 38), function()
    if _G.SavedPos1 then
        local character = LocalPlayer.Character
        if character and character:FindFirstChild("HumanoidRootPart") then
            character.HumanoidRootPart.CFrame = _G.SavedPos1
        end
    end
end)

createButton(TeleportTab, "Save Position 2", UDim2.new(0, 10, 0, 235), UDim2.new(0, 200, 0, 38), function()
    local character = LocalPlayer.Character
    if character and character:FindFirstChild("HumanoidRootPart") then
        _G.SavedPos2 = character.HumanoidRootPart.CFrame
        print("Position 2 saved!")
    end
end)

createButton(TeleportTab, "Load Position 2", UDim2.new(0, 220, 0, 235), UDim2.new(0, 200, 0, 38), function()
    if _G.SavedPos2 then
        local character = LocalPlayer.Character
        if character and character:FindFirstChild("HumanoidRootPart") then
            character.HumanoidRootPart.CFrame = _G.SavedPos2
        end
    end
end)

createSectionHeader(TeleportTab, "World Teleport", UDim2.new(0, 10, 0, 290))

createDropdown(TeleportTab, "Select Location", UDim2.new(0, 10, 0, 325), {"Shop", "Arena", "Boss Room", "Safe Zone", "Secret Area"}, function(option)
    print("Selected Location:", option)
end)

-- ============================================================
-- MISC TAB
-- ============================================================

local MiscTab = Instance.new("ScrollingFrame")
MiscTab.Name = "MiscTab"
MiscTab.Size = UDim2.new(1, -10, 1, -55)
MiscTab.Position = UDim2.new(0, 5, 0, 50)
MiscTab.BackgroundTransparency = 1
MiscTab.ScrollBarThickness = 4
MiscTab.ScrollBarImageColor3 = Color3.fromRGB(120, 80, 200)
MiscTab.CanvasSize = UDim2.new(0, 0, 0, 400)
MiscTab.Visible = false
MiscTab.ZIndex = 12
MiscTab.Parent = RightPanel
tabFrames["Misc"] = MiscTab

createSectionHeader(MiscTab, "Miscellaneous", UDim2.new(0, 10, 0, 5))

createToggle(MiscTab, "Anti-AFK", UDim2.new(0, 10, 0, 40), true, function(state)
    print("Anti-AFK:", state)
end)

createToggle(MiscTab, "Auto-Rejoin", UDim2.new(0, 10, 0, 80), false, function(state)
    print("Auto-Rejoin:", state)
end)

createButton(MiscTab, "Rejoin Server", UDim2.new(0, 10, 0, 120), UDim2.new(0, 200, 0, 38), function()
    game:GetService("TeleportService"):Teleport(game.PlaceId, LocalPlayer)
end)

createButton(MiscTab, "Copy Server Link", UDim2.new(0, 220, 0, 120), UDim2.new(0, 200, 0, 38), function()
    if setclipboard then
        setclipboard("roblox://placeId=" .. game.PlaceId .. "&gameInstanceId=" .. game.JobId)
        print("Server link copied!")
    end
end)

createSectionHeader(MiscTab, "Game Info", UDim2.new(0, 10, 0, 175))

local infoText = Instance.new("TextLabel")
infoText.Size = UDim2.new(1, -20, 0, 100)
infoText.Position = UDim2.new(0, 10, 0, 210)
infoText.BackgroundColor3 = Color3.fromRGB(20, 20, 35)
infoText.BackgroundTransparency = 0.3
infoText.Text = "Game: " .. game:GetService("MarketplaceService"):GetProductInfo(game.PlaceId).Name .. "\nPlaceId: " .. game.PlaceId .. "\nJobId: " .. game.JobId:sub(1, 20) .. "...\nPlayers: " .. #Players:GetPlayers() .. "/" .. Players.MaxPlayers
infoText.TextColor3 = Color3.fromRGB(180, 180, 220)
infoText.TextSize = 13
infoText.Font = Enum.Font.Gotham
infoText.TextXAlignment = Enum.TextXAlignment.Left
infoText.TextYAlignment = Enum.TextYAlignment.Top
infoText.ZIndex = 14
infoText.Parent = MiscTab

local infoCorner = Instance.new("UICorner")
infoCorner.CornerRadius = UDim.new(0, 8)
infoCorner.Parent = infoText

local infoPadding = Instance.new("UIPadding")
infoPadding.PaddingLeft = UDim.new(0, 10)
infoPadding.PaddingTop = UDim.new(0, 8)
infoPadding.Parent = infoText

createButton(MiscTab, "Print Remote Events", UDim2.new(0, 10, 0, 325), UDim2.new(0, 200, 0, 38), function()
    for _, v in ipairs(game:GetService("ReplicatedStorage"):GetDescendants()) do
        if v:IsA("RemoteEvent") or v:IsA("RemoteFunction") then
            print(v:GetFullName())
        end
    end
end)

-- ============================================================
-- SETTINGS TAB
-- ============================================================

local SettingsTab = Instance.new("ScrollingFrame")
SettingsTab.Name = "SettingsTab"
SettingsTab.Size = UDim2.new(1, -10, 1, -55)
SettingsTab.Position = UDim2.new(0, 5, 0, 50)
SettingsTab.BackgroundTransparency = 1
SettingsTab.ScrollBarThickness = 4
SettingsTab.ScrollBarImageColor3 = Color3.fromRGB(120, 80, 200)
SettingsTab.CanvasSize = UDim2.new(0, 0, 0, 350)
SettingsTab.Visible = false
SettingsTab.ZIndex = 12
SettingsTab.Parent = RightPanel
tabFrames["Settings"] = SettingsTab

createSectionHeader(SettingsTab, "UI Settings", UDim2.new(0, 10, 0, 5))

createSlider(SettingsTab, "UI Opacity", UDim2.new(0, 10, 0, 40), 50, 100, 100, function(value)
    MainFrame.BackgroundTransparency = 1 - (value / 100)
end)

createToggle(SettingsTab, "UI Blur Effect", UDim2.new(0, 10, 0, 90), false, function(state)
    if state then
        local blur = Instance.new("BlurEffect")
        blur.Name = "UniversalBlur"
        blur.Size = 10
        blur.Parent = game:GetService("Lighting")
    else
        local blur = game:GetService("Lighting"):FindFirstChild("UniversalBlur")
        if blur then blur:Destroy() end
    end
end)

createDropdown(SettingsTab, "UI Theme", UDim2.new(0, 10, 0, 130), {"Dark (Default)", "Red", "Blue", "Purple"}, function(option)
    print("Theme:", option)
end)

createSectionHeader(SettingsTab, "Keybinds", UDim2.new(0, 10, 0, 185))

local keybindInfo = Instance.new("TextLabel")
keybindInfo.Size = UDim2.new(1, -20, 0, 80)
keybindInfo.Position = UDim2.new(0, 10, 0, 220)
keybindInfo.BackgroundColor3 = Color3.fromRGB(20, 20, 35)
keybindInfo.BackgroundTransparency = 0.3
keybindInfo.Text = "Toggle UI: Right Shift\nToggle Button: Always visible\n\nDrag the circle button to reposition"
keybindInfo.TextColor3 = Color3.fromRGB(180, 180, 220)
keybindInfo.TextSize = 13
keybindInfo.Font = Enum.Font.Gotham
keybindInfo.TextXAlignment = Enum.TextXAlignment.Left
keybindInfo.TextYAlignment = Enum.TextYAlignment.Top
keybindInfo.ZIndex = 14
keybindInfo.Parent = SettingsTab

local keybindCorner = Instance.new("UICorner")
keybindCorner.CornerRadius = UDim.new(0, 8)
keybindCorner.Parent = keybindInfo

local keybindPadding = Instance.new("UIPadding")
keybindPadding.PaddingLeft = UDim.new(0, 10)
keybindPadding.PaddingTop = UDim.new(0, 8)
keybindPadding.Parent = keybindInfo

createButton(SettingsTab, "Destroy UI", UDim2.new(0, 10, 0, 310), UDim2.new(0, 200, 0, 38), function()
    ScreenGui:Destroy()
end)

-- ============================================================
-- TOGGLE / CLOSE / MINIMIZE FUNCTIONALITY
-- ============================================================

local mainVisible = false

local function toggleMain()
    mainVisible = not mainVisible
    
    if mainVisible then
        MainFrame.Visible = true
        MainFrame.Size = UDim2.new(0, 0, 0, 0)
        MainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
        
        TweenService:Create(MainFrame, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
            Size = UDim2.new(0, 750, 0, 500),
            Position = UDim2.new(0.5, -375, 0.5, -250)
        }):Play()
    else
        local closeTween = TweenService:Create(MainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.In), {
            Size = UDim2.new(0, 0, 0, 0),
            Position = UDim2.new(0.5, 0, 0.5, 0)
        })
        closeTween:Play()
        closeTween.Completed:Connect(function()
            MainFrame.Visible = false
        end)
    end
end

-- Toggle button click (only if not dragged far)
local clickStart = nil
ToggleButton.MouseButton1Down:Connect(function()
    clickStart = tick()
end)

ToggleButton.MouseButton1Up:Connect(function()
    if clickStart and (tick() - clickStart) < 0.3 then
        toggleMain()
    end
    clickStart = nil
end)

-- Close button
CloseBtn.MouseButton1Click:Connect(function()
    mainVisible = false
    local closeTween = TweenService:Create(MainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.In), {
        Size = UDim2.new(0, 0, 0, 0),
        Position = UDim2.new(0.5, 0, 0.5, 0)
    })
    closeTween:Play()
    closeTween.Completed:Connect(function()
        MainFrame.Visible = false
    end)
end)

-- Minimize button
MinBtn.MouseButton1Click:Connect(function()
    mainVisible = false
    local minTween = TweenService:Create(MainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
        Size = UDim2.new(0, 750, 0, 0),
        Position = UDim2.new(0.5, -375, 0.5, 0)
    })
    minTween:Play()
    minTween.Completed:Connect(function()
        MainFrame.Visible = false
    end)
end)

-- Tab header update when switching tabs
for tName, _ in pairs(tabButtons) do
    tabButtons[tName].button.MouseButton1Click:Connect(function()
        TabHeader.Text = "Tab: " .. tName
    end)
end

-- Keyboard shortcut to toggle UI (Right Shift)
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if not gameProcessed and input.KeyCode == Enum.KeyCode.RightShift then
        toggleMain()
    end
end)

-- ============================================================
-- ANTI-AFK (Default enabled)
-- ============================================================

spawn(function()
    while wait(60) do
        local virtualUser = game:GetService("VirtualUser")
        pcall(function()
            virtualUser:CaptureController()
            virtualUser:ClickButton2(Vector2.new())
        end)
    end
end)

-- ============================================================
-- NOTIFICATION SYSTEM
-- ============================================================

local function sendNotification(title, text, duration)
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = title or "Universal Hub",
        Text = text or "",
        Duration = duration or 3,
    })
end

-- Send welcome notification after loading
spawn(function()
    wait(4.5)
    pcall(function()
        sendNotification("Universal Hub", "Welcome, " .. LocalPlayer.Name .. "! Press the circle button or Right Shift to toggle.", 5)
    end)
end)

print("═══════════════════════════════════")
print("  Universal Hub Loaded Successfully")
print("  By: DevNameGelo | Powered by: RGC")
print("  Press the circle button to open")
print("  Or press Right Shift")
print("═══════════════════════════════════")
